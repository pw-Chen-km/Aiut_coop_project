# Import

The Import section defines the data fields required for Kanban ingestion, such as file name, Kanban ID, and version number. It also outlines the available import actions, including adding new versions or Kanbans, and explains how default behaviors are determined based on existing system data.
content_type: Field Definitions
question_intents: What fields and actions are involved in the Kanban import process?

## `n_a9a160bbe9886c2ca9fb6ab9` Kanbans:

The section instructs the user to select a Kanban from a drop-down list for adding a new version or enter a name for a new Kanban.
content_type: instruction

## `n_56c882bf689b2fbdf338f12c` Actions*:

The section lists specific actions available for Kanban import, including adding a new version, adding a new Kanban, skipping the import, and the imported status. It explains that the default action depends on whether the Kanban exists in the system and notes that users can manually change these settings.
content_type: Reference
question_intents: What are the available actions for Kanban import?
